// jest-setup.ts
// This file contains setup code that will be executed before running tests
// You can add global setup code here like mock initializations or environment settings

// Mock global objects or APIs if needed
// For example: global.fetch = jest.fn();

// Set test environment variables if needed
process.env.NODE_ENV = "test";
